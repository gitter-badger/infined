sudo: required
language: node_js
node_js:
    - "0.10"
before_script:
    - npm install -g grunt-cli
    - npm install grunt-contrib-sass
    - grunt
    - export PATH=$PATH:$PWD/node_modules/.bin
    - gulp
