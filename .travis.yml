sudo: required
language: node_js
node_js:
    - "0.12"
services:
  - mongodb
before_install:
    - npm install
before_script:
    - npm install grunt-cli
    - npm install grunt-contrib-sass
    - grunt
    - export PATH=$PATH:$PWD/node_modules/.bin
    - gulp
after_install:
    - npm start